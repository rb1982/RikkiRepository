server:
  port: 8282
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rb1982/ConfigRepository.git

management:
  security:
    enabled: false
  endpoints:
    web:
      basePath: /manage
      exposure:
        include: '*'            